[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
pythonpath = .
addopts = -q
markers =
    postgres: requires an external PostgreSQL database with migrations applied
